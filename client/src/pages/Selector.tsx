import { PlatformShell } from "@/components/PlatformShell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { useLanguage } from "@/lib/i18n";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, CircleAlert, ClipboardCheck, Droplets, Loader2, MapPin, Sprout } from "lucide-react";
import { FormEvent, useState } from "react";

type PlannerInput = { country: string; area: string; soilType: string; waterSource: string; goal: string; season: string };
const initial: PlannerInput = { country: "Jordan", area: "", soilType: "", waterSource: "", goal: "", season: "" };

export default function Selector() {
  const { language } = useLanguage();
  const [form, setForm] = useState<PlannerInput>(initial);
  const evaluate = trpc.planner.evaluate.useMutation();
  const update = (field: keyof PlannerInput, value: string) => setForm(previous => ({ ...previous, [field]: value }));
  const labels = language === "ar" ? { country: "الدولة", area: "المساحة", soil: "نوع التربة", water: "مصدر المياه", goal: "الهدف", season: "الموسم", submit: "أنشئ ترشيحًا مبدئيًا", heading: "ماذا أزرع؟", sub: "اجمع بيانات الموقع أولاً، ثم احصل على ترشيحات واضحة مع حدودها." } : { country: "Country", area: "Area", soil: "Soil type", water: "Water source", goal: "Goal", season: "Season", submit: "Create initial recommendations", heading: "What should I grow?", sub: "Gather site data first, then receive clear recommendations with their limits." };
  const submit = (event: FormEvent) => { event.preventDefault(); evaluate.mutate(form); };
  const missing = Object.values(form).filter(value => !value.trim()).length;
  return <PlatformShell title={labels.heading} eyebrow={language === "ar" ? "جامع بيانات ذكي قبل أي توصية" : "Smart data collection before any recommendation"}><main className="container grid gap-6 py-8 lg:grid-cols-[.88fr_1.12fr]"><section className="rounded-[1.6rem] border border-[#35530e]/10 bg-white p-5 shadow-[0_14px_32px_rgba(48,67,22,.05)] sm:p-7"><div className="flex items-start gap-3"><span className="grid size-11 place-items-center rounded-xl bg-[#edf4e5] text-[#35530e]"><ClipboardCheck className="size-5" /></span><div><h2 className="text-xl font-bold text-[#314617]">{language === "ar" ? "بيانات الموقع" : "Site data"}</h2><p className="mt-1 text-sm leading-6 text-[#6b785d]">{labels.sub}</p></div></div><div className="mt-5 flex items-center gap-3 rounded-xl bg-[#f4f7ef] px-4 py-3"><div className="h-2 flex-1 overflow-hidden rounded-full bg-[#dfe8d2]"><div className="h-full rounded-full bg-[#73973a] transition-all" style={{ width: `${Math.round(((6 - missing) / 6) * 100)}%` }} /></div><span className="text-xs font-bold text-[#5c7339]">{Math.round(((6 - missing) / 6) * 100)}%</span></div><form onSubmit={submit} className="mt-6 grid gap-4"><div><Label>{labels.country}</Label><Select value={form.country} onValueChange={value => update("country", value)}><SelectTrigger className="mt-1.5 h-11 rounded-xl"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Jordan">الأردن / Jordan</SelectItem><SelectItem value="Saudi Arabia">السعودية / Saudi Arabia</SelectItem><SelectItem value="UAE">الإمارات / UAE</SelectItem><SelectItem value="Qatar">قطر / Qatar</SelectItem><SelectItem value="Egypt">مصر / Egypt</SelectItem><SelectItem value="Palestine">فلسطين / Palestine</SelectItem></SelectContent></Select></div><div className="grid gap-4 sm:grid-cols-2"><div><Label>{labels.area}</Label><Input value={form.area} onChange={event => update("area", event.target.value)} className="mt-1.5 h-11 rounded-xl" placeholder={language === "ar" ? "مثال: 250 م²" : "e.g., 250 m²"} required /></div><div><Label>{labels.season}</Label><Input value={form.season} onChange={event => update("season", event.target.value)} className="mt-1.5 h-11 rounded-xl" placeholder={language === "ar" ? "مثال: الربيع" : "e.g., spring"} required /></div></div><div><Label>{labels.soil}</Label><Input value={form.soilType} onChange={event => update("soilType", event.target.value)} className="mt-1.5 h-11 rounded-xl" placeholder={language === "ar" ? "مثال: طميية جيدة الصرف" : "e.g., well-drained loam"} required /></div><div><Label>{labels.water}</Label><Input value={form.waterSource} onChange={event => update("waterSource", event.target.value)} className="mt-1.5 h-11 rounded-xl" placeholder={language === "ar" ? "مثال: شبكة + خزان" : "e.g., mains + tank"} required /></div><div><Label>{labels.goal}</Label><Input value={form.goal} onChange={event => update("goal", event.target.value)} className="mt-1.5 h-11 rounded-xl" placeholder={language === "ar" ? "مثال: إنتاج منزلي اقتصادي" : "e.g., economical home production"} required /></div><Button type="submit" disabled={evaluate.isPending || missing > 0} className="mt-2 h-11 rounded-xl bg-[#35530e] text-white hover:bg-[#294108]">{evaluate.isPending ? <Loader2 className="size-4 animate-spin" /> : <Sprout className="size-4" />}{labels.submit}</Button></form></section>
  <section className="min-w-0"><div className="rounded-[1.6rem] border border-[#35530e]/10 bg-[#f2f6ec] p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold tracking-[.14em] text-[#78924a]">{language === "ar" ? "نتيجة قابلة للتفسير" : "EXPLAINABLE RESULT"}</p><h2 className="mt-2 text-xl font-bold text-[#314617]">{language === "ar" ? "الترشيحات المبدئية" : "Initial recommendations"}</h2></div><span className="grid size-11 place-items-center rounded-xl bg-white text-[#35530e]"><MapPin className="size-5" /></span></div>{!evaluate.data && <div className="mt-8 rounded-2xl border border-dashed border-[#b8cca0] bg-white/60 p-7 text-center"><Droplets className="mx-auto size-8 text-[#7f9d4d]" /><p className="mt-3 text-sm leading-6 text-[#68775a]">{language === "ar" ? "أكمل بيانات الموقع لنوضح ما يلائم المكان وما يحتاج معلومات إضافية." : "Complete site data to clarify what fits the place and what needs more information."}</p></div>}{evaluate.data && <div className="mt-6"><p className="rounded-xl bg-white/75 p-3 text-sm leading-6 text-[#536745]">{evaluate.data.basis}</p><div className="mt-4 space-y-3">{evaluate.data.entries.map(entry => <article key={entry.plant} className="rounded-2xl bg-white p-4 shadow-sm"><div className="flex items-center justify-between gap-3"><h3 className="font-bold text-[#314617]">{entry.plant}</h3><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${entry.tier === "recommended" ? "bg-[#edf5e4] text-[#4c7620]" : entry.tier === "good" ? "bg-[#f7f2e5] text-[#95742c]" : "bg-[#fff0eb] text-[#ac5434]"}`}>{entry.tier === "recommended" ? (language === "ar" ? "موصى به" : "Recommended") : entry.tier === "good" ? (language === "ar" ? "خيار جيد" : "Good choice") : (language === "ar" ? "تحقق أولاً" : "Check first")}</span></div><p className="mt-2 text-sm leading-6 text-[#68775a]">{entry.reason}</p><div className="mt-3 grid gap-2 rounded-xl bg-[#f7f9f4] p-3 text-xs leading-5 text-[#60714d]"><p><strong className="text-[#3f5720]">{language === "ar" ? "الكمية: " : "Quantity: "}</strong>{entry.quantities}</p><p><strong className="text-[#3f5720]">{language === "ar" ? "التكلفة: " : "Cost: "}</strong>{entry.costs}</p><p><strong className="text-[#3f5720]">{language === "ar" ? "الري والعناية: " : "Irrigation & care: "}</strong>{entry.irrigation}</p></div></article>)}</div><div className="mt-4 flex items-start gap-2 rounded-xl border border-[#e7cf9e] bg-[#fffaf0] p-3 text-xs leading-5 text-[#806536]"><CircleAlert className="mt-0.5 size-4 shrink-0" />{language === "ar" ? "المعلومات الناقصة قبل التخطيط التفصيلي: " : "Missing before detailed planning: "}{evaluate.data.missing.join("، ")}</div></div>}</div></section></main></PlatformShell>;
}
